tosca_definitions_version: tosca_simple_profile_for_nfv_1_0_0
description: A generic VNFD for containers.

topology_template:
  node_templates:
    VDU1:
      capabilities:
        nfv_compute:
          properties:
            mem_size: 512 MB
            num_cpus: 1
      properties:
        type: container
        image: gvsouza/nham
        availability_zone: nova

    resiliency:
      num_backups: 4
      cooldown: 5
      vnf_level:
        type: multisync  # options: active-standby, active-active, multisync
      infra_level:
        type: local           # options: local, remote, None
        remote_site: x.y.z.w  # apply only for remote
